{
  "version": 2,
  "buildCommand": "npm run build && npm install --save-dev webpack webpack-cli webpack-node-externals nodemon ts-loader typescript",
  "outputDirectory": "dist",
  "routes": [
    {
      "src": "/(.*)",
      "dest": "server.js"
    }
  ]
}
